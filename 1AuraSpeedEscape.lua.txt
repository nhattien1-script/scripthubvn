-- Khởi tạo menu
local player = game.Players.LocalPlayer
local mouse = player:GetMouse()
local gui = Instance.new("ScreenGui")
local frame = Instance.new("Frame")
local button = Instance.new("TextButton")
local title = Instance.new("TextLabel")

-- Cấu hình GUI
gui.Name = "FarmWinMenu"
gui.Parent = player:WaitForChild("PlayerGui")
gui.ResetOnSpawn = false

-- Tạo Frame chính
frame.Name = "MainFrame"
frame.Parent = gui
frame.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
frame.BorderSizePixel = 2
frame.BorderColor3 = Color3.fromRGB(255, 0, 0)
frame.Position = UDim2.new(0.5, -150, 0.5, -75)
frame.Size = UDim2.new(0, 300, 0, 150)
frame.Active = true
frame.Draggable = true
frame.ClipsDescendants = true

-- Tạo Title với tên mới
title.Name = "Title"
title.Parent = frame
title.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
title.BorderSizePixel = 1
title.BorderColor3 = Color3.fromRGB(255, 215, 0)
title.Size = UDim2.new(1, 0, 0, 30)
title.Text = " +1 Aura Speed Escape by nhattien:> 🚀"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.Font = Enum.Font.GothamBold
title.TextSize = 16
title.TextWrapped = true

-- Tạo Button Teleport
button.Name = "TeleportButton"
button.Parent = frame
button.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
button.BorderSizePixel = 2
button.BorderColor3 = Color3.fromRGB(0, 255, 255)
button.Position = UDim2.new(0.5, -100, 0.5, -20)
button.Size = UDim2.new(0, 200, 0, 40)
button.Text = "🌟 TELEPORT TO WIN (7k5) 🌟"
button.TextColor3 = Color3.fromRGB(255, 255, 255)
button.Font = Enum.Font.GothamBold
button.TextSize = 16
button.TextWrapped = true

-- Hiệu ứng lấp lánh ánh kim đỏ-xanh
local sparkle = Instance.new("Frame")
sparkle.Name = "SparkleEffect"
sparkle.Parent = frame
sparkle.BackgroundTransparency = 1
sparkle.Size = UDim2.new(1, 0, 1, 0)
sparkle.ZIndex = 2

-- Tạo các điểm sáng lấp lánh
local sparks = {}
for i = 1, 15 do
    local spark = Instance.new("Frame")
    spark.Name = "Spark" .. i
    spark.Parent = sparkle
    spark.BackgroundColor3 = i % 2 == 0 and Color3.fromRGB(255, 0, 0) or Color3.fromRGB(0, 255, 255)
    spark.BorderSizePixel = 0
    spark.Size = UDim2.new(0, math.random(2, 5), 0, math.random(2, 5))
    spark.Position = UDim2.new(math.random(), 0, math.random(), 0)
    spark.Rotation = math.random(0, 360)
    spark.BackgroundTransparency = 0.3
    table.insert(sparks, spark)
end

-- Animation lấp lánh
game:GetService("TweenService"):Create(sparkle, TweenInfo.new(1, Enum.EasingStyle.Linear, Enum.EasingDirection.Out, -1, true), {
    BackgroundTransparency = 0.8
}):Play()

for _, spark in ipairs(sparks) do
    local tween = game:GetService("TweenService"):Create(spark, TweenInfo.new(math.random(1, 2), Enum.EasingStyle.Linear, Enum.EasingDirection.Out, -1, true), {
        BackgroundTransparency = math.random(0, 5) / 10,
        Rotation = spark.Rotation + 180,
        Size = UDim2.new(0, math.random(3, 8), 0, math.random(3, 8))
    })
    tween:Play()
end

-- Hiệu ứng viền lấp lánh
local borderEffect = Instance.new("Frame")
borderEffect.Name = "BorderEffect"
borderEffect.Parent = frame
borderEffect.BackgroundTransparency = 1
borderEffect.Size = UDim2.new(1, 0, 1, 0)
borderEffect.ZIndex = 3

local borderTween = game:GetService("TweenService"):Create(borderEffect, TweenInfo.new(0.5, Enum.EasingStyle.Linear, Enum.EasingDirection.Out, -1, true), {
    BorderSizePixel = 3,
    BorderColor3 = Color3.fromRGB(255, 0, 0)
})
borderTween:Play()

-- Function Teleport
local function teleportToFarmWin()
    local character = player.Character
    if character and character:FindFirstChild("HumanoidRootPart") then
        -- Hiệu ứng khi teleport
        local humanoidRootPart = character.HumanoidRootPart
        local oldPosition = humanoidRootPart.Position
        
        -- Tạo hiệu ứng khói
        local smoke = Instance.new("Part")
        smoke.Name = "SmokeEffect"
        smoke.Parent = workspace
        smoke.Position = oldPosition
        smoke.Anchored = true
        smoke.CanCollide = false
        smoke.Transparency = 1
        smoke.Size = Vector3.new(1, 1, 1)
        
        local smokeEffect = Instance.new("Smoke", smoke)
        smokeEffect.Enabled = true
        smokeEffect.RiseVelocity = 10
        smokeEffect.Size = NumberRange.new(2, 5)
        smokeEffect.Opacity = NumberRange.new(0.5, 1)
        smokeEffect.Color = Color3.new(1, 0, 0)
        
        -- Teleport
        humanoidRootPart.CFrame = CFrame.new(4993.3, 217.3, 52.6)
        
        -- Xóa hiệu ứng sau 2 giây
        game:GetService("Debris"):AddItem(smoke, 2)
        
        -- Thông báo
        print("✨ Đã teleport đến Farm Win! ✨")
    else
        warn("Không tìm thấy nhân vật!")
    end
end

-- Gán sự kiện cho button
button.MouseButton1Click:Connect(teleportToFarmWin)

-- Hiệu ứng hover cho button
button.MouseEnter:Connect(function()
    game:GetService("TweenService"):Create(button, TweenInfo.new(0.2), {
        BackgroundColor3 = Color3.fromRGB(65, 65, 75),
        TextColor3 = Color3.fromRGB(255, 215, 0)
    }):Play()
end)

button.MouseLeave:Connect(function()
    game:GetService("TweenService"):Create(button, TweenInfo.new(0.2), {
        BackgroundColor3 = Color3.fromRGB(45, 45, 55),
        TextColor3 = Color3.fromRGB(255, 255, 255)
    }):Play()
end)

-- Thêm nút đóng menu
local closeButton = Instance.new("TextButton")
closeButton.Name = "CloseButton"
closeButton.Parent = frame
closeButton.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
closeButton.BorderSizePixel = 1
closeButton.BorderColor3 = Color3.fromRGB(255, 255, 255)
closeButton.Position = UDim2.new(1, -25, 0, 5)
closeButton.Size = UDim2.new(0, 20, 0, 20)
closeButton.Text = "X"
closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
closeButton.Font = Enum.Font.GothamBold
closeButton.TextSize = 14

closeButton.MouseButton1Click:Connect(function()
    gui:Destroy()
end)

-- Hiệu ứng hover cho nút đóng
closeButton.MouseEnter:Connect(function()
    closeButton.BackgroundColor3 = Color3.fromRGB(255, 100, 100)
end)

closeButton.MouseLeave:Connect(function()
    closeButton.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
end)

print("✅ +1 Aura Speed Escape by nhattien:> đã được tạo! Kéo thả menu để di chuyển.")